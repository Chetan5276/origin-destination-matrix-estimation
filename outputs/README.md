# Outputs

Local pipeline artifacts (synthetic OD tensors, turning counts, trained models, reports).

This directory is gitignored except for this README. Re-run the Phase 1–3 commands in the project README to regenerate.
